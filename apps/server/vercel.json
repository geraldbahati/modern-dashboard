{
  "buildCommand": "cd ../.. && pnpm turbo run build --filter=server...",
  "installCommand": "pnpm install --filter=server...",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
